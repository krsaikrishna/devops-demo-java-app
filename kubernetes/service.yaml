apiVersion: v1
kind: Service
metadata:
  name: hello-app-service
  namespace: default
  labels:
    app: hello-app
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: hello-app
